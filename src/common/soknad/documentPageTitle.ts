const APP_TITLE = 'Søknad - midlertidig kompensasjon for selvstendig næringsdrivende og frilansere';

export const createDocumentPageTitle = (title: string): string => {
    return `${title} - ${APP_TITLE}`;
};
